import VueCroppa, { Croppa, type CroppaModelValue } from 'vue-croppa'

const component: typeof Croppa = Croppa
const plugin: typeof VueCroppa = VueCroppa
const model: CroppaModelValue | null = null

void [component, plugin, model]
